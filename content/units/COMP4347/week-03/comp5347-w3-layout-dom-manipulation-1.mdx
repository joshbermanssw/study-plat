---
title: COMP5347_W3_Layout_DOM_Manipulation (1)
unit: COMP4347
week: 3
type: reading
status: synced
sourceFile: COMP5347_W3_Layout_DOM_Manipulation (1).pdf
module: Manual upload
localFile: data/sources/COMP4347/COMP5347_W3_Layout_DOM_Manipulation_(1).pdf
fetchedAt: 2026-05-21T07:08:27.307Z
fileSize: 6093544
fileContentType: application/pdf
---
> Manually uploaded COMP5347_W3_Layout_DOM_Manipulation (1).pdf. Edit this note any time.

{/* canvas-extract-start */}
<details>
<summary>Source text (auto-extracted · 49 pages)</summary>

```text
The University of Sydney Page 1 COMP5347: Web Application Development CSS Layout & Interactive JS Ori Livson School of Computer Science The University of Sydney Page 2 COMMONWEALTH OF Copyright Regulations 1969 WARNING This material has been reproduced and communicated to you by or on behalf of the University of Sydney pursuant to Part VB of the Copyright Act 1968 (the Act). The material in this communication may be subject to copyright under the Act. Any further reproduction or communication of this material by you may be the subject of copyright protection under the Act. Do not remove this notice. The University of Sydney Page 3 Outline – More CSS – Layout – Responsive Design – More JavaScript – DOM Manipulation – Events COMP5347 Web Application Development The University of Sydney Page 4 What is Layout? The layout of an element is where that object sits on a webpage, especially, relative to other objects. Examples of layout requirements of an object X include: – “X must be in the center of its parent Y, horizontally”* – “X must start 30px from the left of its parent Y” – “The height of X must take up a 3rd of the height of its parent Y” – “The children of Y must each have equal width and together fill all the space available” * Does X start at the center of Y or should the center of X be at the center of Y? It gets tricky! The University of Sydney Page 5 Coordinate Based vs Flow Based Layout Coordinate Based Elements are arranged using distances, often relative to an ancestor element. E.g., exact pixel measurements or % of the ancestor’s size. Uses the CSS position property Flow Based Elements are arranged based on the relationships and order between surrounding elements. E.g., stacking elements rightwards so that one element’s position depends on what elements come before it in HTML Uses the CSS display property The two can be mixed. The University of Sydney Page 6 Coordinate Based Layout The aim of coordinate based layout is to position an element X by setting some combination of the CSS properties: top: where does the top of X’s element box start? bottom: where does the bottom of X… left: where does the left of X… right: where does the right of X… Say we set top: 10px; Then what is X 10px from? This depends on the CSS position property. The University of Sydney Page 7 Position: Relative If we set position: relative; then the following for “Example Element”, the following CSS { position: relative; top: 10px; left: 10px } Means the element box for “Example Element”: horizontally starts 10px from the left (blue) of where it would have otherwise (yellow). And likewise, vertically starts 10px from the top of where it would have otherwise. Note: for position: relative, top supersedes bottom and left supersedes right The University of Sydney Page 8 Example: Note: because by default #element starts at the top left corner, if we replace left with right, it’ll start left, beyond the visible window The University of Sydney Page 9 Position: Absolute If we set position: absolute; then the top/bottom/left/right shift occurs with respect to the element box of the closest ancestor with a position value* *If one cannot be found, the <html> element is used by default. The University of Sydney Page 10 Example: #child starts 150px left of window because: #parent starts 100px left of window And #child starts 50px Left of #parent The University of Sydney Page 11 Can combine top & bot or left & right to auto-set the width & height The University of Sydney Page 12 Overlap & Z-Index Coordinate based layouts Can cause overlaps. These can be resolved with the z-index CSS property. Higher z-index elements get visualised over lower z-index elements. The University of Sydney Page 13 Hiding Elements Sometimes you need to hide elements from a webpage. The following CSS property: value pairs make an object invisible but differ in space taken and interactivity. CSS Property Takes Space? Interactive? display: none; No No visibility: hidden; Yes No opacity: 0; Yes Yes The University of Sydney Page 14 Flow Based Layout - Flex A Flex Container stacks children on the main direction. Flex Items can have absolute sizes or flex sizes, which automatically fills available space in the flexbox. Items can be set to start stacking in the cross direction once it’s filled up the main (i.e., wrapping) Ref: Fireship’s CSS Flexbox in 100s (https://www.youtube.com/watch?v=K74l26pE4YA) flex-direction: row flex-direction: column The University of Sydney Page 15 flex: 1 means grow on the main axis (width in this case) to 1 unit / sum(all units in row) of the available width in the row (i.e., 25% width here)* * flex specifically means flex-grow when it has 1 argument - see MDN The University of Sydney Page 16 An item having flex-basis: 20% means it initially takes that width if possible. Here, it’s possible with wrapping. The University of Sydney Page 17 It can be set to flex-grow beyond the basis if possible The University of Sydney Page 18 If the items can’t fit with that basis (i.e., because it can’t wrap), it can be configured to flex-shrink. The University of Sydney Page 19 justify-content: positions items along the main axis align-items: positions items along the main axis The University of Sydney Page 20 align-content: positions whole stacks (rows here) along the cross axis The University of Sydney Page 21 Flow Based Layout - Grid Note: don’t use inline styles – I’m just using them to make the screenshots smaller. CSS Grid is like Flex but 2D and supports templates with fixed item counts per axis The University of Sydney Page 22 Media Queries Media Queries are a type of CSS selector that target specific device features or browser window states, e.g., – The width of the visible screen / window. – Whether the device is in portrait or landscape mode – Whether the device has a pointer, supports hovering over elements, etc. These features help determine whether your website is running on a phone, table, laptop, ultrawide monitor, etc. Different layouts and sizing choices are appropriate for different devices. The University of Sydney Page 23 Example: The University of Sydney Page 24 Common Device Width Ranges Ref: https://www.w3schools.com/css/css_rwd_mediaqueries.asp Mobile first approach: Can also search for common min-width to max-width ranges The University of Sydney Page 25 Alternative: CSS Functions When it’s not the device type that’s the issue but general width constraints, consider using the CSS min, max or clamp functions, e.g., The University of Sydney Page 26 Recommended Resources 1. W3 schools then MDN interactive docs 2. Short video summaries: – Coordinate Based (Coding2Go) – Flow Based • Block & Inline (Coding2Go) • Flex (Fireship 100s) • Grid (Fireship 100s) 3. Long video tutorials – Flex (Coding2Go) – Grid (Coding2Go) – CSS Functions vs Media Queries (Coding2Go) The University of Sydney Page 27 Outline – More CSS – Layout – Responsive Design – More JavaScript – DOM Manipulation – Events COMP5347 Web Application Development The University of Sydney Page 28 Recall: HTML as a tree COMP5347 Web Application Development <body> <p> This is some <strong>text</strong> </p> <h1>Title goes here </h1> <div> <p> This is <span>important</span> </p> </div> </body> child parent sibling descendants ancestor <body> <p> <div><h1> <strong> <span> <p> children siblings descendants ancestors The University of Sydney Page 29 The DOM (Document Object Model) The DOM is an API for representing a webpage’s HTML as a tree. This tree is often also called “The DOM”. JavaScript comes with built-in functions for reading and manipulating the DOM. Manipulating the DOM allows the client to alter the webpage without reloading it. The University of Sydney Page 30 Example: Note: script’s JS runs as soon as the browser parses the <script> tag. You can verify this by The document object represents the webpage the browser has parsed up to that point; not what is visible or interactive! We will need events to have such guarantees. The University of Sydney Page 31 How do we know this? Move the script above the <p> tag. Then, debug the JS in the sources tab of the DevTools demoParagraph is null, therefore it is not in the html that the browser has parsed yet. The University of Sydney Page 32 Events Events are signals triggered by user actions or browser activity* Event handlers can be written to run every time a particular signal is fired. They allow your JS to react to things like: – Button clicks – Form submissions – Selection changes in a drop down – The page load – Window resizing Etc. *Or JS libraries themselves The University of Sydney Page 33 Examples of Events Event Property Event Name Target Trigger window.onload load window All HTML elements have been parsed into the DOM + External resources loaded and run + CSS layouts and sized calculated. button.onclick click <button> Button pressed (i.e., mouse down + mouse up events finished firing) form.onsubmit submit <form> Corresponding <input type=“submit”> button pressed. select.onchange change <select> Fires when selected <option> is changed. The University of Sydney Page 34 Example: Assigning window.onload a function means that function is called every time “load” event signal is fired for the built-in window object. The function caller always passes an event object as an argument which contains information about the signal that triggered the handler. E.g., a key press event object would contain the character that the user pressed. The University of Sydney Page 35 InnerText vs InnerHTML innerHTML collects all the HTML inside the element, including tags innerText builds up all the text, excluding tags The University of Sydney Page 36 This works all the way down the tree! The University of Sydney Page 37 How to add to the DOM To add to the DOM within an element X, one needs to manipulate the innerHTML of X. The simplest (not-recommended) way is to write in a string. Why? It gets messy, it’s hard to ensure the added html is valid, The University of Sydney Page 38 Why is this not recommended? – It gets messy – It’s hard to ensure the html string is valid – It can be vulnerable to hacking E.g., malicious JS sneaking into the string* *We will discuss this in Week 11 The University of Sydney Page 39 Programmatic DOM Construction JS has built in functions for creating HTML elements and nesting them. This is cleaner, less likely to produce invalid HTML and we can validate the parts (e.g., the URL) The University of Sydney Page 40 Elements can be programmatically nested too Then functions can be written to generalise DOM construction The University of Sydney Page 41 Modifying Styles COMP5347 Web Application Development const commentTag = document.getElementById("specificTag"); commentTag.style.backgroundColour = "#FFFF00"; commentTag.style.borderWidth="3px"; const commentTag = document.getElementById("specificTag"); commentTag.className = "someClassName"; Also The University of Sydney Page 42 How to read the DOM To manipulate the DOM effectively, we need to be able to efficiently select where in the HTML tree we want to make our changes. Function What it does document.getElementById(id) Returns the single element with the specified id. document.querySelector(selector) Returns the first element that matches a CSS selector. element.querySelector(selector) Finds the first matching descendant of a specific element using a CSS selector. We can query for single nodes, e.g., See week 2 slides for examples of CSS selectors The University of Sydney Page 43 Function What it does document.getElementsByClassName(className) Returns a live HTMLCollection of all elements with the given class name. document.getElementsByTagName(tagName) Returns a live HTMLCollection of elements with the specified HTML tag (e.g., div, p). document.getElementsByName(name) Returns a NodeList of elements with the specified name attribute (commonly used in forms). document.querySelectorAll(selector) Returns a static NodeList of all elements matching a CSS selector. element.querySelectorAll(selector) Finds all matching descendants of a specific element using a CSS selector. element.getElementsByClassName(className) Returns a live collection of descendant elements with the given class. element.getElementsByTagName(tagName) Returns a live collection of descendant elements with the given tag. We can also query for lists of nodes Note: HTMLCollection automatically syncs with DOM changes and NodeList doesn’t but can be iterated over with JS forEach The University of Sydney Page 44 Essential Node Properties COMP5347 Web Application Development Property Description attributes Collection of node attributes childNodes A NodeList of child nodes for this node firstChild First child node of this node lastChild Last child of this node nextSibling Next sibling node for this node nodeName Name of the node nodeType Type of the node nodeValue Value of the node parentNode Parent node for this node previousSibling Previous sibling node for this node The University of Sydney Page 45 DOM edits using node properties Remove by reference Insert by reference The University of Sydney Page 46 JS Extras: Arrow Functions Arrow functions are a compact way to define functions like so We can also use them as straight unnamed values, e.g., The University of Sydney Page 47 However, while these two expressions have the same input output behaviour. There are some differences, such as: Hoisting, meaning functions can be called in an earlier line to their definition. E.g., Also, arrow functions don’t support the this keyword i.e., the JS object version for the function The University of Sydney Page 48 JS Extras: Alternate Handler Syntax Event handlers can be added using the event name, rather than the special property name, e.g., The University of Sydney Page 49 Next week: The Browser & The Rendering Process
```

</details>
{/* canvas-extract-end */}
